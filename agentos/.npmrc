# agentos/ is a standalone Vite SPA, not a pnpm workspace member. Tell pnpm
# to ignore the parent ComputerAgent monorepo workspace so install/build
# operations only touch this directory's package.json + pnpm-lock.yaml.
ignore-workspace=true
